{
  "name": "circular-progress",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/circular-progress/src",
  "projectType": "library",
  "tags": ["publishable", "type:ui-component", "readme"],
  "targets": {
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/circular-progress/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "build": {
      "executor": "@nx/vite:build",
      "dependsOn": [
        {
          "projects": "readme",
          "target": "generate-markdown"
        }
      ],
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/packages/circular-progress"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/packages/circular-progress"
      }
    },
    "npm-deploy": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public",
        "distFolderPath": "dist/packages/circular-progress"
      },
      "dependsOn": ["pre-npm-deploy-build-production"]
    },
    "pre-npm-deploy-build-production": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run circular-progress:build:production"
      }
    }
  }
}
